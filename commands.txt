mkdir modern-jenkins && cd modern-jenkins
cat <<EOF > Dockerfile
FROM jenkins/jenkins:lts
USER root
RUN apt-get update && apt-get install -y docker.io && rm -rf /var/lib/apt/lists/*
EOF


docker build -t my-modern-jenkins .

docker run -d \
  -p 8080:8080 \
  -p 50000:50000 \
  --name my-jenkins \
  --user root \
  -v /var/jenkins_home:/var/jenkins_home \
  -v /var/run/docker.sock:/var/run/docker.sock \
  my-modern-jenkins

  
 
docker pipeline install plugins in jenkins
 
# ==========================================
# WAKING UP WORKSPACE AFTER RESTART
# ==========================================

# Step 0: Ensure Docker Desktop is launched and running on your computer.

# Step 1: Wake up Kubernetes (Minikube cluster container)
minikube start --driver=docker

# Step 2: Wake up Jenkins (container name is 'my-jenkins')
docker start my-jenkins

# Step 3: Wake up Local Testing Container (optional, container name is 'my-running-app')
docker start my-running-app

# Step 4: Verify everything is running side-by-side in Docker
docker ps

# Step 5: Verify your Kubernetes pods are running safely
kubectl get pods

# Step 6: Get access links to view your apps in the browser:
# - Jenkins UI: Open http://localhost:8080
# - Local Docker App: Open http://localhost:8081
# - Kubernetes App: Run command below to get the URL
minikube service simple-app-service --url

